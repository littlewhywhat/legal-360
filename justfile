# Legal 360 — root command runner (install: https://github.com/casey/just)
# Recipes are stubs until apps are scaffolded.

dev:
    @echo "TODO: run web + api (e.g. parallel just web / just api)"

web:
    @echo "TODO: cd apps/web && pnpm dev"

api:
    @echo "TODO: cd apps/api && uv run …"

test:
    @echo "TODO: web + api tests"
